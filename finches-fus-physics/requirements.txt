# FINCHES-FUS-Physics: Dual-stack physics pipeline
# Python 3.9+ required

# ── Core scientific computing ──
numpy>=1.22.0
scipy>=1.9.0
pandas>=1.4.0

# ── Visualization ──
matplotlib>=3.5.0

# ── Jupyter notebooks ──
jupyter>=1.0.0
jupyterlab>=3.0.0
ipykernel>=6.0.0

# ── Progress bars ──
tqdm>=4.60.0

# ── Graph + topology (Python layer) ──
networkx>=2.8.0

# ── Topological data analysis ──
# giotto-tda: persistent homology, Betti curves, persistence landscapes
# This is HIGH LEVERAGE for capturing global structure beyond local clustering
giotto-tda>=0.6.0

# ── ML on topology (optional, for hybrid models) ──
# torch>=2.0.0               # uncomment when ready for GNN experiments
# torch-geometric>=2.3.0     # uncomment for PyTorch Geometric (GNN on graphs)

# ── Julia interop ──
# juliacall: Python ↔ Julia bridge (requires Julia 1.9+ installed separately)
juliacall>=0.9.0
